query GetShop($namespace: String, $heroKey: String!) {
    shop {
        id
        name
        hero: metafield(namespace: $namespace, key: $heroKey) {
            reference {
                ... on MediaImage {
                    image {
                        url
                        altText
                        width
                        height
                    }
                }
            }
        }
    }
}

