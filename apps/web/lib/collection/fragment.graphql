fragment Filters_Product on ProductConnection {
    filters {
        id
        label
        type
        values {
            id
            label
            count
            input
        }
    }
}