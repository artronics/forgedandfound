The content of this directory MUST be all symlinks.
Intellij Workspace feature can not add *individual* files to the workspace. 
So, I've created this directory to gather files from the root directory so the entire directory can be added to the workspace in one go.

DO NOT create actual files in here. ONLY symlinks from root.